# TODO List

1. Evaluate the need for `Orchestrator`
   - Maybe can use it to build off of `services/cm`
   - Idea:
     1. make it a multi-bot service that improves bot collaboration (as a collective) instead of single-bot orchestration
        - reason: too many edge cases to handle for single-bot orchestration, and it is more beneficial to focus on multi-bot collaboration instead
     2. rename to `orchestration` or `coordination`
     3. use it for more complex multi-bot coordination tasks
