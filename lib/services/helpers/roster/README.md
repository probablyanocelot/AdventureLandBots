# helpers/roster

Service-owned roster/party metadata helpers.

## Public API

- `getRosterNames()`
- `getRosterMeta(rosterNames)`
- `getRosterSnapshot()`
- `compareByMeta(aName, bName, meta)`
- `readLuck(obj)`
